@model IEnumerable<GymVidaYSaludWEB.Entities.DatosCliente>

@{
    ViewData["Title"] = "Consultar Clientes";
}

<h1>Clientes</h1>

<p>
    <a asp-action="RegistrarCliente">Registrar nuevo cliente</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cedula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreCompleto)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Correo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cedula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCompleto)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Correo)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { cedula=item.Cedula }) |
                @Html.ActionLink("Información", "ConsultarUnCliente", new { cedula=item.Cedula }) |
                @Html.ActionLink("Borrar", "EliminarCliente", new { cedula=item.Cedula})
            </td>
        </tr>
}
    </tbody>
</table>
